apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: guestbook
spec:
  generators:
  - list:
      elements:
      - cluster: development
        url: https://development.vcluster-development:443
      - cluster: staging
        url: https://staging.vcluster-staging:443
      - cluster: production
        url: https://production.vcluster-production:443
  template:
    metadata:
      name: '{{cluster}}-guestbook'
    spec:
      project: default
      source:
        repoURL: https://github.com/GoogleCloudPlatform/kubernetes-engine-samples
        targetRevision: HEAD
        path: quickstarts/guestbook
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            maxDuration: 3m0s
            factor: 2
      destination:
        server: '{{url}}'
        namespace: guestbook